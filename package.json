{
  "name": "service_reminder_common",
  "version": "1.1.2",
  "description": "",
  "homepage": "https://github.com/Kashyap2210/service_reminder_common#readme",
  "bugs": {
    "url": "https://github.com/Kashyap2210/service_reminder_common/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Kashyap2210/service_reminder_common.git"
  },
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsup",
    "prepublishOnly": "npm run build",
    "full-publish": "npm run build && npm version patch && git push --follow-tags && npm publish",
    "major-publish": "npm version major",
    "minor-publish": "npm version minor",
    "publish:beta": "npm run build && npm version prerelease --preid=beta && git push --follow-tags && npm publish --tag beta",
    "link": "npm run build && npm link",
    "preversion": "npm run build",
    "postversion": "git push --follow-tags"
  },
  "dependencies": {
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
